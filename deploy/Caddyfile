# Certificat HTTPS obtenu et renouvelé automatiquement. WebSockets (temps réel) transmis sans réglage.
{$DOMAIN} {
	encode zstd gzip
	reverse_proxy app:8080
}

# Redirige www vers le domaine principal
www.{$DOMAIN} {
	redir https://{$DOMAIN}{uri} permanent
}
